"""
Schema Generators

Modules for generating code from YAML schemas:
- python_fieldspec: Generate Python FieldSpec definitions
- prisma: Generate Prisma schema (future)
- typescript: Generate TypeScript types (future)
"""
